export interface DragLabel {
    labelIndex: number,
    x: number;
    y: number;
}

export interface Line {
    x0: number;
    y0: number;
    x1: number;
    y1: number;
}

export interface Point {
    x: number;
    y: number;
}